function  questiontest(params) {
    let someValues = new Array(20)
    // console.log(someValues);
    someValues.forEach((element, index) => {
        console.log(1);
        console.log(element);
        // console.log(`Current index: ${index}`);
        // console.log(element);
    });
}
questiontest()